DockerFile:
  --> simple teext file that consists of instructions to build docker images.
  --> Syntax:
          --> $ docker build -t[image name]:[tag]
          --> $ docker build -t centos-httpd:latest
